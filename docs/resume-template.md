# 简历模板 — AI Agent方向

> 针对AI Agent/推荐系统/大模型工程师岗位优化

---

## ⚠️ 使用前必读

**本文件下面两个模板里的数字，多数没有实测支撑。** 它们是早期写的，
后来逐一核对代码时发现对不上。

**直接照抄会在面试里被问穿。** 具体哪些不能用：

| 说法 | 实际情况 |
|---|---|
| 端到端延迟 **P99 < 2s** | 从未达到。实测 **p95 是 3,997 ms** |
| 推荐 **CTR 提升 15%** | 没有任何数据支撑 —— A/B 引擎返回的 `config` 从未被读取，实验目前不影响行为 |
| **Redis Sorted Set** 实时特征 / 特征更新 < 100ms | **从未实现**（曾有一份未接线的实现，已删除）。画像走的是内置兜底数据 |
| **Milvus 向量检索** / 百万级 ANN 召回 < 50ms | `pymilvus` 在依赖里，但**代码里从未 import** |
| **MySQL** | 库存现在走 **SQLite + MCP** |
| **Java / Go 三语言实现** | 已删除，只保留 Python |
| LLM 成功率 **85% → 99%** | 没有实测依据 |
| 50 QPS / 可用性 99.9% | 没有压测依据 |

### ✅ 请改用这一版（每个数字都可在仓库里找到出处）

见 [README.md 的「简历写法」一节](../README.md#-简历写法直接复制) —— 那版是
二次开发后重写的，数字全部来自 `eval_results/` 里可复现的评测报告。

**两版之间最重要的差别**：新版**主动展示了两处"我否决/推翻了自己"**
（否决现成 MCP Server、按实测数据推翻自己的推理决策）。
这比任何正向指标都更能证明工程判断力 —— 而且面试官没法用"你怎么测的"问倒你。

---

## 模板一：应届/初级工程师（初版，数字待替换）

```
姓名 | 联系方式 | GitHub: github.com/bcefghj

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
教育背景
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
XX大学 | 计算机科学与技术 | 本科/硕士 | 20XX-XX

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
项目经验
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

多Agent电商推荐与营销系统 | 个人项目 | 2026.09-2026.10
GitHub: github.com/bcefghj/multi-agent-ecommerce

• 设计并实现基于Supervisor模式的多Agent协同架构,含用户画像、
  商品推荐、营销文案、库存决策4个专业Agent,采用并行分发+聚合的编排模式

• 为系统实现运行时保障层:请求级trace贯穿、真超时、滑动窗口熔断与两级降级。
  故障注入验证:注入超时后连续4次请求仍全部返回HTTP 200

• 基于MCP SDK v2实现Server+Client+Host三端:自建2个Server、
  库存Agent作为Client消费WMS、并实现多轮tool-calling loop的运营Copilot

• 构建评测闭环(10条golden set+确定性门禁+非门禁LLM裁判),
  全链路p50从48015ms降至2788ms(约17倍),单次成本降至$0.0006

• 集成LLM实现个性化营销文案生成,5套Prompt模板覆盖新客/VIP/
  价格敏感等用户分群,广告法合规率100%

技术栈: MCP · LangGraph · FastAPI · SQLite · Redis · structlog · Pytest
```

> 注意：上面已把**已删除的技术栈（Java/Go/Spring AI/Milvus/MySQL）
> 和没有实测依据的数字（P99<2s、CTR+15%）去掉**，换成了可验证的。
> 但更推荐直接用 README 里那一版 —— 它的结构更适合讲面试故事。

---

## 模板二：社招/中级工程师（突出工程能力）

```
多Agent电商推荐与营销系统 | 技术负责人 | 2026.01-2026.04

• 架构设计: 基于Supervisor模式设计4-Agent并行编排架构,
  LangGraph状态图实现两阶段并行(画像||召回 → 重排||库存 → 文案),
  系统吞吐量50 QPS,P99延迟<2s

• 稳定性工程: 实现Agent级别的重试(指数退避)/超时/降级/熔断四层
  防护,LLM调用成功率从85%提升至99%,系统可用性99.9%

• 数据工程: Redis Feature Store支持实时特征(滑动窗口),
  离线+在线特征合并,Milvus向量检索支持百万级商品ANN召回(<50ms)

• 实验平台: Thompson Sampling A/B引擎,支持三层实验正交,
  实验周期较传统A/B缩短50%,推荐CTR提升15%

• 多语言交付: Python/Java/Go三语言实现+Docker一键部署,
  降低跨团队技术栈接入门槛
```

---

## 简历注意事项

### DO（推荐做法）
1. **量化成果**: CTR提升15%、延迟<2s、成功率99%
2. **突出架构决策**: 为什么选Supervisor而不是Handoffs
3. **体现工程能力**: 重试/超时/降级/监控
4. **关联业务价值**: 缺货推荐率从12%降至0.5%

### DON'T（常见错误）
1. ❌ "使用了LangGraph框架" — 太笼统,要说具体怎么用的
2. ❌ "实现了推荐系统" — 要说具体策略(多路召回+LLM重排)
3. ❌ 不写数字 — 必须有量化指标
4. ❌ 简历超过2页
5. ❌ 把API Key/密码写在简历或GitHub上

---

## 针对不同岗位的简历调整

### AI Agent工程师
重点: Multi-Agent架构、ReAct模式、Prompt工程、Agent稳定性

### 推荐系统工程师
重点: 多路召回、LLM重排、实时特征、A/B测试

### 后端工程师
重点: 并行编排(asyncio/CompletableFuture/goroutine)、
      Redis、微服务、Docker部署

### 大模型应用工程师
重点: LLM集成、Prompt模板设计、合规校验、Token成本控制
