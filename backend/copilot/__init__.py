"""
运营 Copilot —— 本项目的「MCP Host」角色。

    它在 MCP 三端里的位置
    ────────────────────
    Server  recommend_server / wms_server   把能力挂出去 / 提供数据
    Client  inventory_agent                 按名字写死地调某个工具
    Host    ← 这里                          拿着模型，让它【自己决定】调哪些工具

    为什么单独一个包，不放进 harness/
    ────────────────────────────────
    harness 必须能被【确定性主链路】import 而不拖进一个对话循环。
    把 Copilot 放进 harness/ 会让"做推荐"和"聊天"耦合在一起。

    为什么主推荐链路不用这个模式
    ──────────────────────────
    库存这类场景必须【确定性】：同样的输入必须给同样的输出。
    让模型自主决策会引入不确定性，而它换不来任何好处 ——
    那些工具本来就该在固定时机被调用。

    所以本项目刻意分成两条路径：
        确定性主链路  4 个 Agent + 写死的工具调用
        自主决策      仅限运营 Copilot（人问问题，模型自己找答案）
"""
